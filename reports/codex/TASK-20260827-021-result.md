# TASK-20260827-021 结果报告

## 结论

两次独立 clone rehearsal 全部通过，安全且可重复的 reconciliation 路径已经在 disposable PostgreSQL 上得到验证：

- 22 个 legacy FK 与 canonical FK 的结构语义逐项完全一致。
- 4 个 `decision_logs` JSON server default 可安全恢复为 `'[]'::json`。
- 两套 fresh clone 均达到 canonical 0019：`missing=0 / unexpected=0 / divergent=0`。
- 两套 clone 均成功执行 `stamp 0019` 与 `upgrade 0020`。
- 两套 clone 均达到 canonical 0020：`missing=0 / unexpected=0 / divergent=0`。
- 0020 的 `uq_seasons_competition_label (competition_id, label)` 正常创建。
- downgrade 到 0019、严格比对、再 upgrade 到 0020 的恢复演练通过。
- 两轮最终 schema、业务数据和 sequence state 完全一致。

当前已在 **Production Database Approval Gate** 停止。生产数据库没有发生任何 schema 写入，TASK-20260826-020 仍然禁止执行。

## 任务与 Git

- Task ID：`TASK-20260827-021`
- Branch：`codex/TASK-20260827-021`
- Base：`origin/master@d7a25fd45b126b27e5c8d9a5f9e361b1760aaedb`
- PR：未创建
- Merge：未执行
- Production deployment：未执行

## Production 只读基线与最终复核

| 项目 | 演练前 | 演练后 |
|---|---:|---:|
| `alembic_version` | `0002` | `0002` |
| canonical `fk_*` FK 名称 | 0 | 0 |
| legacy `*_fkey` FK 名称 | 24 | 24 |
| FK 总数 | 24 | 24 |
| 4 个 JSON default | 均缺失 | 均缺失 |
| `uq_seasons_competition_label` | 不存在 | 不存在 |

生产 schema 写入：**0**。生产 `stamp / upgrade / downgrade`：**均未执行**。

## Backup 与 Clone 方法

生产 backup 使用 PostgreSQL 16.14 的只读 logical dump：

- custom-format full dump：`backups/TASK-20260827-021/production.dump`
  - 大小：545,291 bytes
  - SHA-256：`F17F2987B8013A5616C7F4AC343D55B117F2507F8849D19E836FC120BFFD95A9`
- schema-only dump：`backups/TASK-20260827-021/production-schema.sql`
  - 大小：32,912 bytes
  - SHA-256：`312A345E7A088D9A92B4AD70145FFD37F2FF0B26E7849923CB8E529C376A7C03`
- `pg_restore --list` 验证通过，archive 共 125 个 TOC entries。

Clone 使用独立 `postgres:16-alpine` 容器 `football-reconcile-021`，仅绑定 `127.0.0.1:55432`，没有复用 production volume。数据库包括：

- `football_reconcile_canonical_0019`
- `football_reconcile_canonical_0020`
- `football_reconcile_r1`
- `football_reconcile_r2`

reconciliation 工具对 mutating target 有硬保护：只接受 loopback host 且数据库名必须以 `football_reconcile_` 开头，因此本任务分支上的工具不能误写 production。

## 22 个 FK 逐项结果

所有条目均满足：local/ref columns 完全一致、`ON UPDATE NO ACTION`、non-deferrable、initially immediate。除 `lineup_players.lineup_id` 为 `ON DELETE CASCADE` 外，其余均为 `ON DELETE NO ACTION`。

| # | Table / local columns | Reference | Delete | Legacy → Canonical | 结果 |
|---:|---|---|---|---|---|
| 1 | `seasons(competition_id)` | `competitions(id)` | NO ACTION | `seasons_competition_id_fkey` → `fk_seasons_competition` | exact |
| 2 | `fixtures(competition_id)` | `competitions(id)` | NO ACTION | `fixtures_competition_id_fkey` → `fk_fixtures_competition` | exact |
| 3 | `fixtures(season_id)` | `seasons(id)` | NO ACTION | `fixtures_season_id_fkey` → `fk_fixtures_season` | exact |
| 4 | `fixtures(home_team_id)` | `teams(id)` | NO ACTION | `fixtures_home_team_id_fkey` → `fk_fixtures_home_team` | exact |
| 5 | `fixtures(away_team_id)` | `teams(id)` | NO ACTION | `fixtures_away_team_id_fkey` → `fk_fixtures_away_team` | exact |
| 6 | `predictions(fixture_id)` | `fixtures(id)` | NO ACTION | `predictions_fixture_id_fkey` → `fk_predictions_fixture` | exact |
| 7 | `value_bets(fixture_id)` | `fixtures(id)` | NO ACTION | `value_bets_fixture_id_fkey` → `fk_value_bets_fixture` | exact |
| 8 | `value_bets(bookmaker_id)` | `bookmakers(id)` | NO ACTION | `value_bets_bookmaker_id_fkey` → `fk_value_bets_bookmaker` | exact |
| 9 | `decision_logs(fixture_id)` | `fixtures(id)` | NO ACTION | `decision_logs_fixture_id_fkey` → `fk_decision_logs_fixture` | exact |
| 10 | `decision_logs(value_bet_id)` | `value_bets(id)` | NO ACTION | `decision_logs_value_bet_id_fkey` → `fk_decision_logs_value_bet` | exact |
| 11 | `odds_snapshots(fixture_id)` | `fixtures(id)` | NO ACTION | `odds_snapshots_fixture_id_fkey` → `fk_odds_snapshots_fixture` | exact |
| 12 | `odds_snapshots(bookmaker_id)` | `bookmakers(id)` | NO ACTION | `odds_snapshots_bookmaker_id_fkey` → `fk_odds_snapshots_bookmaker` | exact |
| 13 | `team_match_statistics(fixture_id)` | `fixtures(id)` | NO ACTION | `team_match_statistics_fixture_id_fkey` → `fk_team_match_statistics_fixture` | exact |
| 14 | `team_match_statistics(team_id)` | `teams(id)` | NO ACTION | `team_match_statistics_team_id_fkey` → `fk_team_match_statistics_team` | exact |
| 15 | `player_availability_observations(fixture_id)` | `fixtures(id)` | NO ACTION | `player_availability_observations_fixture_id_fkey` → `fk_player_availability_fixture` | exact |
| 16 | `player_availability_observations(team_id)` | `teams(id)` | NO ACTION | `player_availability_observations_team_id_fkey` → `fk_player_availability_team` | exact |
| 17 | `players(team_id)` | `teams(id)` | NO ACTION | `players_team_id_fkey` → `fk_players_team` | exact |
| 18 | `player_availability_observations(player_id)` | `players(id)` | NO ACTION | `player_availability_observations_player_id_fkey` → `fk_player_availability_player` | exact |
| 19 | `lineups(fixture_id)` | `fixtures(id)` | NO ACTION | `lineups_fixture_id_fkey` → `fk_lineups_fixture` | exact |
| 20 | `lineups(team_id)` | `teams(id)` | NO ACTION | `lineups_team_id_fkey` → `fk_lineups_team` | exact |
| 21 | `lineup_players(lineup_id)` | `lineups(id)` | CASCADE | `lineup_players_lineup_id_fkey` → `fk_lineup_players_lineup` | exact |
| 22 | `lineup_players(player_id)` | `players(id)` | NO ACTION | `lineup_players_player_id_fkey` → `fk_lineup_players_player` | exact |

reconciliation 在执行任何 DDL 前一次性验证全部 22 条；任一条缺失或语义不同都会抛出错误并回滚。第二次运行全部为 no-op，证明幂等。

## 4 个 default 结果

以下列均被确认类型为 `json`，原值为无 server default；clone 上恢复为 `'[]'::json`：

- `decision_logs.supporting_evidence`
- `decision_logs.risks`
- `decision_logs.rejected_alternatives`
- `decision_logs.change_conditions`

第二次 reconciliation 没有重复修改。

## Strict schema diff

比对范围：tables、columns、types、nullable、defaults、identity/generated flags、indexes、unique constraints、foreign keys、check constraints、constraint names、sequences。

| 阶段 | Missing | Unexpected | Divergent | Exact |
|---|---:|---:|---:|---|
| R1 原始 restore vs canonical 0019 | 22 | 22 | 4 | no |
| R1 reconciliation vs canonical 0019 | 0 | 0 | 0 | yes |
| R1 stamp 0019 + upgrade 0020 vs canonical 0020 | 0 | 0 | 0 | yes |
| R1 downgrade 0019 vs canonical 0019 | 0 | 0 | 0 | yes |
| R1 re-upgrade 0020 vs canonical 0020 | 0 | 0 | 0 | yes |
| R2 原始 restore vs canonical 0019 | 22 | 22 | 4 | no |
| R2 reconciliation vs canonical 0019 | 0 | 0 | 0 | yes |
| R2 stamp 0019 + upgrade 0020 vs canonical 0020 | 0 | 0 | 0 | yes |
| R2 final vs R1 final | 0 | 0 | 0 | yes |

两轮 normalized data-only dump 的 SHA-256 均为：

`15dbb026c22755bb522b2fef5171a67978825e567078a039b885a78c8dae66a0`

原始 plain dump 的差异仅为 `pg_dump` 每次随机生成的 `\restrict/\unrestrict` token；过滤该非数据元信息后完全一致。

## Stamp、0020 与 rollback

- R1：`0002 → stamp 0019 → upgrade 0020` 成功。
- R2：`0002 → stamp 0019 → upgrade 0020` 成功。
- `uq_seasons_competition_label UNIQUE (competition_id, label)` 正常创建。
- R1：`downgrade 0019` 后 exact 0019，再 `upgrade 0020` 后 exact 0020。
- reconciliation 本身位于单一事务中，任一前置验证或 DDL 失败时整笔回滚。
- 事务提交后的最终恢复权威仍是已验证 custom-format full backup。

## 测试

- Focused reconciliation tests：**14 passed**
- Black：通过
- Ruff：通过
- MyPy：通过（`--explicit-package-bases`，因为 `scripts/` 不是 Python package）
- 完整 unit collection：642
- 完整 unit run：641 passed，1 个与本任务无关的既有时间敏感失败
- 排除该已证明的基线失败后：**641 passed**

既有失败：

`tests/unit/test_odds_api_security.py::test_paid_rate_limiter_adopts_server_capacity_and_has_no_daily_cap`

该测试将 reset 固定为 `2026-08-03T18:28:52Z`；当前为 2026-08-27，limiter 因 reset 已过而正确补满至 10000，旧断言仍期待 9999。本分支对该测试及 odds limiter 的 diff 均为空，因此没有在数据库任务中越权修复。

## 预计 production downtime

- 建议维护窗口：**5 分钟**。
- 预计写暂停：**2–5 分钟**。
- logical backup 可在进入写暂停前在线完成。
- 真正耗时取决于 22 次 FK rename 与 4 次 default DDL 的锁获取；生产执行前必须确认没有长事务，并设置短 `lock_timeout`。

## 未来 production 执行步骤（本任务未执行）

以下仅是经 clone rehearsal 得出的未来 runbook。它必须在用户明确批准 **Production Database Approval**、批准生产部署该分支内容、确认 production backup 可恢复并暂停所有写入后执行。

```powershell
cd C:\Users\ruowa\Projects\football-agent

# 1. 维护前备份与可恢复性检查
docker compose exec -T postgres pg_dump -U football -d football -Fc -f /tmp/pre_reconcile.dump
docker compose cp postgres:/tmp/pre_reconcile.dump .\backups\production-pre-reconcile.dump
docker compose exec -T postgres pg_dump -U football -d football --schema-only -f /tmp/pre_reconcile_schema.sql
docker compose cp postgres:/tmp/pre_reconcile_schema.sql .\backups\production-pre-reconcile-schema.sql
docker compose exec -T postgres pg_restore --list /tmp/pre_reconcile.dump

# 2. 在独立 clone 再执行一次本报告中的 audit/reconcile/compare，结果必须仍为 exact。
# 3. 停止所有 API、worker 与 Windows scheduler 写入；确认无运行中的 daily_job/pre_kickoff 和无长事务。

# 4. 使用经过审批的 production reconciliation executor 执行同一事务：
#    - 一次性验证 22 个 FK 全部语义
#    - rename 22 个 FK
#    - SET DEFAULT '[]'::json 到 4 列
#    - 任一失败则 ROLLBACK
# 注意：当前脚本有 clone-only 硬保护；生产模式必须在单独审批中显式解锁，不能绕过。

# 5. strict compare 必须达到 exact 0019 后才允许：
docker compose exec -T api alembic stamp 0019
docker compose exec -T api alembic current
docker compose exec -T api alembic upgrade 0020
docker compose exec -T api alembic current
docker compose exec -T api alembic check

# 6. 再做 strict compare，必须为 exact 0020；随后恢复服务并观察。
```

生产 reconciliation executor 的实际解锁必须是下一次 Production Database Approval 的一部分。当前工具故意拒绝 production DSN，因此不存在通过改 DSN 绕开 Approval Gate 的路径。

如果 reconciliation 事务尚未提交，失败会自动 rollback。如果已提交但 stamp/upgrade 未完成，必须保持写冻结，执行经过审批的 reverse rename/default transaction，或从 verified full backup restore；如果 0020 已创建后需要回退，先在写冻结状态 `alembic downgrade 0019`，再执行 reverse reconciliation 并恢复 ledger `0002`。任何 production downgrade/stamp 同样需要新的明确批准。

## TASK-020 与 TASK-018

- TASK-20260826-020：**现在仍不允许执行**。
- 只有生产 reconciliation、exact 0019、stamp 0019、upgrade 0020、exact 0020 全部在审批下成功后，TASK-020 才可解除阻塞。
- TASK-20260826-018：原任务保持 `do_not_merge`。其中 0020 season unique migration 的语义可在生产 reconciliation 后重新审查并复用；不能直接复活或合并原分支。

## 文件、写入与 Frozen

变更文件：

- `alembic/versions/0020_add_season_natural_key.py`
- `scripts/alembic_ledger_reconciliation.py`
- `tests/unit/test_alembic_ledger_reconciliation.py`
- task/report/handoff 文件

数据库写入：仅 disposable clones。API calls：0。生产影响：无。

Frozen 项确认未变：model weights、alpha、EV threshold、confidence threshold、Kelly formula/cap、Recommendation Gate、production whitelist、betting decision rules。

## Approval Gate

**STOP：等待用户 Production Database Approval。**

不得在当前状态执行 production ALTER/CREATE/DROP、production stamp/upgrade/downgrade、TASK-020、Five-Team Canary、backfill、daily_job、pre_kickoff、merge 或 deployment。
