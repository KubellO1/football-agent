# TASK-20260830-032 结果报告

## 结论

Alembic 0021 的两轮 production-clone rehearsal、downgrade/recovery 和重复 upgrade 均通过，结果确定且一致。0021 本身没有数据迁移，不会删除或修改已有赔率快照，也不会猜测历史 provider；旧数据的 provenance 保持 `NULL`。

**Clone rehearsal 技术结论：PASS。** 当前进入 Production Database Approval Gate，必须停止，不能自动升级 production、运行真实 snapshot Canary、部署或合并。

## Task / Git

- Task ID：`TASK-20260830-032`
- Branch：`codex/TASK-20260830-032`
- Base：`c37e2b5504cb2e71445493e58e16fa009b72e4c2`
- TASK-031 implementation：`7f8c1ea7359ba11be0d0124a7aaf6c5dd2ec8199`
- 本任务没有修改 0021 或生产代码；只增加任务归档、报告和 handoff。

## 0021 精确结构变更

Migration：`alembic/versions/0021_add_odds_snapshot_provider_provenance.py`

- revision：`0021`
- down_revision：`0020`
- table：`odds_snapshots`
- 新增 `provider_source VARCHAR(40) NULL`，无 server default
- 新增 `provider_event_id VARCHAR(128) NULL`，无 server default
- 新增普通 btree index：
  `ix_odds_snapshots_provider_event(provider_source, provider_event_id)`
- 未新增/修改 unique、FK 或 check constraint
- downgrade：先删除索引，再删除两个字段

### 现有 snapshot 处理

1. 0021 不执行 `UPDATE`/`DELETE`，不会改动或删除已有赔率数据。
2. 旧 snapshot 无法可信推断 provider，因此两个新字段保留 `NULL`。
3. 不使用 `UNKNOWN`、`LEGACY` 或某个具体 provider 伪造来源。
4. 当前 production `odds_snapshots=0`，迁移不存在历史行误标风险。
5. nullable/no-default `ADD COLUMN` 仍需短暂表锁；普通 `CREATE INDEX` 会扫描表并可能阻塞写入。clone 实测约 1.1–1.6 秒，建议预留 5 分钟维护窗口完成备份和全部核验。

## Production 备份与基线

- 方法：PostgreSQL custom-format `pg_dump`，包含 schema、data、sequences 和 `alembic_version`
- 文件：`backups/TASK-20260830-032/production_0020_20260830_192838.dump`
- 大小：887,067 bytes
- SHA-256：`ACA31B8DA841F0D8D5EBA3AF1D1D4315A12E50F6357A1A4B9A6ECBF1FB85A84A`
- `pg_restore -l`：PASS
- Production PostgreSQL：16.14
- Revision：0020
- Fixtures：7,624
- Odds snapshots：0
- Bookmakers：0
- Predictions（备份时）：549
- Decision logs：0
- 重复 snapshot 自然键组：0
- public sequences：0
- 0020 provenance 字段数：0

两个 clone 均从同一份原始 production backup 全新创建，没有从第一次 rehearsal 的结果复制。

## Schema 精确比较

使用规范化 schema-only dump 比较，并排除每次 dump 随机生成的 psql `restrict/unrestrict` 标记。

- exact 0020 hash：`CC7F48A45F44D09B2A03A9DF9748C272E84DE57D4197991F8146B27E12DF79BF`
- exact 0021 hash：`D5B4418C03DE750AB56F9AAE8F1E6895986933214C5F013B4C5565C7322AB202`

两轮最终 0021 hash 完全相同；每次 downgrade 后的 0020 hash 也与初始 0020 完全相同。

## 两轮 Rehearsal

| Clone | 第一次 upgrade | downgrade | 第二次 upgrade | 最终 revision | exact 0021 |
|---|---:|---:|---:|---|---|
| `football_task_032_r1_test` | PASS / 1541ms | PASS / 1115ms | PASS / 1315ms | 0021 | 是 |
| `football_task_032_r2_test` | PASS / 1126ms | PASS / 1189ms | PASS / 1115ms | 0021 | 是 |

每个阶段均验证：

- `alembic current` 正确
- 0021 时 `alembic check` PASS
- 两列名称、类型、nullable、default 精确
- 索引名称、列顺序与定义精确
- odds snapshot 行数 0 → 0
- 重复自然键组 0 → 0
- 非 NULL provenance 行数 0
- sequence 变化 0
- downgrade 后 schema exact 0020
- re-upgrade 后 schema exact 0021

## Application Compatibility

在独立 clone 上验证 TASK-031 路径：

- Odds-API.io snapshot 正确携带 `provider_source/provider_event_id`
- The Odds API fallback snapshot 正确携带 provenance
- provider event ID 保持 provider-scoped
- 跨 provider 的同名 Bet365 仍归一为同一 bookmaker，不能错误计为两家
- fixture / market / bookmaker / source 可审计
- 现有 EV/analysis pipeline 可读取 snapshot
- model/Gate 行为无变化

Focused compatibility：**47 passed**。

## 测试与质量

- Focused/application compatibility：47 passed
- Full unit：686 passed
- Full integration：103 passed
- Incremental Ruff（CI changed-file 范围）：PASS
- Incremental MyPy（CI 参数，9 个 app 文件）：PASS
- `git diff --check`：PASS

首次在带 7,624 条 production fixtures 的 clone 上运行完整 integration 时，`test_backfill_range_and_idempotent` 因假设空库而看到 7,627 条 fixture；这属于测试隔离前置条件，不是 0021 regression。随后在 fresh empty disposable PostgreSQL 上重跑，**103/103 PASS**。

### 独立质量阻塞

TASK-031 继承的 `tests/unit/test_prioritized_odds_fallback.py` 在当前 Black 版本下会被重新格式化。该文件由提交 `7f8c1ea` 引入，不是 TASK-032 改动；本任务遵守范围，没有混入无关格式修复。TASK-031 进入 PR/CI 前应单独按 CI Black 版本格式化该文件。

全仓 MyPy 的 72 个既有错误及全仓 Ruff 的 229 个既有发现也不属于本任务；GitHub CI 使用 changed-file 增量范围，本任务按相同参数复核通过 Ruff/MyPy。

## Production 最终只读核对

- Revision：仍为 0020
- Odds snapshots：0
- 重复自然键组：0
- Provenance 字段：0
- 本任务 production writes：0

长时间 rehearsal 期间，production predictions 从备份时 549 增至最终只读核对时 561。这是已有外部调度独立运行造成，不是本任务写入。本任务没有启动 `daily_job`、`pre_kickoff` 或其他 production job。

## Rollback / Recovery

两次 `0021 → 0020` downgrade 均成功，原有 snapshot 行数、重复数、core table 数据和 sequence 状态保持不变，schema 恢复 exact 0020。随后再次 upgrade 均恢复 exact 0021。

生产执行时仍应优先使用迁移前的新鲜 custom-format backup 作为最终恢复依据；downgrade 只会删除 provenance 字段，因此如果 production 0021 后产生了 provenance，downgrade 会丢失这些新字段值。

## 未来生产执行程序（仅建议，未执行）

只有在 TASK-031 代码/0021 已完成 review、merge/deploy 审批，并再次获得 Production Database Approval 后：

1. 临时暂停所有 PostgreSQL 写入任务，不永久修改任务定义。
2. 获取并验证新鲜 custom-format production backup。
3. 只读 preflight：revision 必须为 0020，schema 和关键计数必须符合 rehearsal 输入。
4. 从包含未修改 0021 的已审核应用环境执行：`alembic upgrade 0021`。
5. 执行 `alembic current` 和 `alembic check`。
6. 核对 exact 0021 columns/index、行数、重复自然键、sequence，以及历史 provenance 仍为 NULL。
7. 全部通过后恢复计划任务；失败则按批准的恢复决策使用新鲜 backup，或执行已 rehearsal 的 downgrade。

## TASK-031 Canary 状态

Clone rehearsal 证明 0021 技术上可迁移，但 production Canary **现在仍未获授权**。只有以下条件全部完成后才可进入独立 Canary Approval Gate：

- TASK-031 代码和 migration 已审核、合并并获部署批准
- production 0021 获单独批准并完成 post-migration exact 验证
- TASK-031 继承 Black 格式漂移已处理
- 用户明确批准最多 5 场 live snapshot Canary

## Frozen / 影响

- Production DB writes：0
- External API calls：0
- Production jobs run：0
- Production deployment：0
- Windows scheduled-task changes：0
- Frozen changes：0

**STOP：等待 Production Database Approval，不自动迁移、合并、部署或运行 Canary。**
