# TASK-20260827-022 — Audit Stop Report

## 结果

任务已创建独立分支并完成前置审计，但在代码修改前遇到范围阻塞，因此 STOP。

## 审计事实

- Branch：`codex/TASK-20260827-022`
- Base：`origin/master@d7a25fd45b126b27e5c8d9a5f9e361b1760aaedb`
- 最新 master Alembic head：`0019`
- master 中没有 `0020_add_season_natural_key.py`
- 已在 production 执行的 0020 仅存在于：`codex/TASK-20260827-021@ec88789`
- 0020 定义：
  - table：`seasons`
  - columns：`competition_id`, `label`
  - constraint：`uq_seasons_competition_label`
- `SeasonORM` 当前没有该 `UniqueConstraint`

## 为什么必须停止

用户要求同时满足：从最新 master 创建分支、只修复 ORM metadata、不创建 migration、并验证 isolated migrations `0001 → 0020`。但 master 没有 0020，所以这些条件目前无法同时成立。

在未获得明确授权前：

- 不能把 TASK-021 全部提交历史带入本分支；
- 不能从 TASK-021 暗中复制 0020 migration；
- 不能伪造 `0001 → 0020` 验证；
- 不能只验证到 0019 后声称任务完成。

## 建议的最小授权

允许将 `ec88789` 中已经生产验证的、内容不变的 `alembic/versions/0020_add_season_natural_key.py` 携带到本分支。之后本任务的实际代码改动仍只包含：

```python
UniqueConstraint(
    "competition_id",
    "label",
    name="uq_seasons_competition_label",
)
```

然后完成 isolated `0001 → 0020`、`alembic check`、autogenerate diff、测试和质量检查。

## 安全确认

- Production writes：0
- Disposable DB writes：0
- API calls：0
- TASK-20260826-020：未执行
- Frozen：全部未变
- Master：未合并
