# TASK-20260917-067 result

## Outcome

Prediction decision persistence is now replay-idempotent without a schema change and without
changing model or betting behavior. Identical semantic decisions reuse an existing row; a new
checkpoint, model version, material input, or decision payload remains a distinct audit record.

`PREDICTION_NATURAL_KEY=fixture_id + workflow/checkpoint + daily run_date + prediction_version + model_version + material input fingerprint + persisted decision payload fingerprint`

`SCHEMA_CHANGE_REQUIRED=no`

The semantic key is hashed and converted to a deterministic UUIDv5 primary key. PostgreSQL uses
`INSERT ... ON CONFLICT DO NOTHING`, so concurrent identical attempts are safe. New rows also
store an auditable `provider_sources.decision_identity` object. Existing random-UUID rows remain
unchanged and are reused through exact decision/context matching.

## Contract audit

- `DailyProductionRun`: context is `daily + DAILY + run_date`.
- `pre_kickoff`: context is explicitly passed as `T90`, `T60`, or `T30`.
- Persistence timestamps are excluded from identity because they change on replay.
- Material model inputs, lineup admission, prediction/model versions, and persisted decision
  fields are included.
- T-90, T-60, and T-30 are legitimate separate decisions and are never collapsed.
- Changed model version or material input produces a new row even for the same fixture.

## Production read-only audit

- Revision: `0021`
- Predictions: 571, all `decision`; aggregate rows: 0
- Fixtures represented: 155
- WATCH: 262; NO_ODDS: 309; BET: 0
- Fixtures with multiple decision rows: 146
- Fixtures with legitimate multiple contexts: 127
- Ignoring checkpoint creates a false duplicate picture: 155 groups / 291 extra rows.
- After classifying DAILY/T90/T60/T30, historical semantic duplicates remain: 34 groups / 52
  extra rows.
- Historical rows changed or deleted: 0

The historical 52-row excess is documented only. This task intentionally does not clean or
rewrite production history.

## Production-clone rehearsal

A read-only production `pg_dump` was restored to disposable database
`football_task067_clone_test`.

| Metric | Before | First replay delta | Second replay delta |
|---|---:|---:|---:|
| fixtures | 7,800 | 0 | 0 |
| odds snapshots | 276 | 0 | 0 |
| predictions | 571 | 0 | 0 |

Both replays analyzed 177 fixtures. The first replay reused the two TASK-066 legacy decision rows;
the second also inserted zero. New identity-key duplicates and odds natural-key duplicates were
both zero. No external APIs were called.

## Tests

- Focused identity/logger/DailyTopPicks tests: 11 passed
- Full unit suite: 692 passed
- Full integration suite: 107 passed
- Ruff: PASS
- Black: PASS
- MyPy with CI parameter `--follow-imports=silent`: PASS
- `git diff --check`: PASS

Coverage includes WATCH replay, BET/NO_BET preservation, distinct checkpoints, changed
model/input context, legacy-row reuse, and concurrent insertion.

## Safety

- Production DB writes: 0
- Production DailyProductionRun executions: 0
- External API calls: 0
- Scheduler changes: 0
- Schema/migration changes: 0
- 0022: frozen
- Frozen logic diff: 0

## Required result fields

- `WATCH_REPLAY_IDEMPOTENT=yes`
- `DISTINCT_CHECKPOINTS_PRESERVED=yes`
- `CONCURRENT_REPLAY_SAFE=yes`
- `CLONE_FIRST_RUN_PREDICTIONS=0 new; 571 total`
- `CLONE_SECOND_RUN_NEW_PREDICTIONS=0`
- `DUPLICATES=0 new; 52 historical semantic extra rows preserved`
- `FROZEN_DIFF=0`
- `READY_FOR_CONTROLLED_DAILY_REPLAY=yes`

STOP before production replay, merge, deployment, migration, or Scheduler change.
