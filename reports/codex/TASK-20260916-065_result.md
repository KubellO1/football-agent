# TASK-20260916-065 — Clean Production Runtime Preparation

## Result

`READY_FOR_CONTROLLED_DAILY_CANARY`

The persistent API now runs from the approved clean release at commit
`6cc6c8f03bcf4f537db8ab49711402e71155de70`, and the Scheduler release virtual environment was
rebuilt from the project manifest. All read-only runtime checks passed. No Scheduler writer was
enabled or executed.

## Persistent API

| Check | Result |
|---|---|
| Container | `football-api`, running |
| Release mount | `C:/Users/ruowa/Projects/football-agent-release-dashboard-6cc6c8f` → `/app` |
| Release label | `6cc6c8f03bcf4f537db8ab49711402e71155de70` |
| Image | `sha256:a0a65ef04a6a5ebf634144581115a381567eda2b567908301a9cda4b32d9974a` |
| `/api/v1/health` | HTTP 200, `status=ok` |
| `/api/v1/ready` | HTTP 200, database/Redis both `ok` |
| Tracebacks / missing modules | 0 / 0 |
| Credential leak markers | 0 |
| External football API URL markers | 0 |

The final image was built from a sanitized `git archive` context containing tracked release files
only. `.env` and local virtual environments were absent from the image context and image. The
initial ordinary Compose build was aborted before image export when the missing `.dockerignore`
was detected; its unused build cache was pruned before the sanitized rebuild.

## Scheduler runtime

- Python: `C:/Users/ruowa/Projects/football-agent-release-dashboard-6cc6c8f/.venv/Scripts/python.exe`
- Python version: `3.12.14`
- Installed project: `football-agent 0.1.0`
- `tzdata`: `2026.4`
- `pip check`: PASS
- `ZoneInfo("Europe/Paris")`: PASS
- Scheduler and both odds-provider imports: PASS
- Manifest SHA-256: `FEBF2741D671BC4E513CC7145C138F6E83215AED0CD519DFD487104AB8AF2919`
- Installed dependency snapshot SHA-256:
  `D023DC8C41087B3FBD85F5CA2BF82A38EBC1E5F0B29F92F24706F2F26EA4BD2A`

Administrator verification found all nine canonical tasks. Every executable points to the rebuilt
`.venv`, every working directory points to the clean release, and all eight unique commands resolve
in the Scheduler registry.

| Task | Command | Enabled state |
|---|---|---|
| ProviderHealthCheck | `provider_health` | Enabled / Ready |
| DailyProductionRun | `daily_job` | **Disabled** |
| ProductionRecoveryCheck | `production_recovery` | **Disabled** |
| PreKickoffValidation | `pre_kickoff` | **Disabled** |
| DashboardRefresh-1300 | `dashboard_refresh` | Enabled / Ready |
| DashboardRefresh-1700 | `dashboard_refresh` | Enabled / Ready |
| SettlementFallback | `settlement` | **Disabled** |
| DailyPerformanceReport | `daily_report` | Enabled / Ready |
| WeeklyPerformanceReport | `weekly_report` | Enabled / Ready |

Scheduler definition changes: 0. Writer processes: 0. Writer tasks enabled by this task: 0.

## PostgreSQL and Redis

| Check | Result |
|---|---:|
| PostgreSQL / Redis | healthy / healthy |
| Production / repository Alembic | 0021 / 0021 |
| `alembic check` | clean |
| Canonical tables | 18 |
| Fixtures | 7,624 → 7,624 |
| Predictions | 569 → 569 |
| Bookmakers | 0 → 0 |
| Odds snapshots | 0 → 0 |
| FK orphans / duplicate groups | 0 / 0 |
| PostgreSQL write delta | **0** |

The read-only verifier still matches TASK-063's recovered schema hash
`18C334664F110CD165757DB43F271A31462600AF1941DAC4329DF5ED7224360F`.
Alembic 0022 remains frozen. Known data gaps were not repaired.

## Safety and forensic preservation

TASK-063 forensic assets remain present and unmodified.

- External football/data API calls: 0
- Production database writes: 0
- Writer jobs run: 0
- Collectors/backfills/models/historical repairs: 0
- Frozen changes: 0

## Non-blocking hardening notes

1. No dependency lock file exists. The environment was installed only from the approved
   `pyproject.toml`, passed `pip check`, and its exact installed set was preserved as a hashed
   `pip freeze` artifact.
2. No tracked `.dockerignore` exists. This deployment avoided secret inclusion by building from a
   sanitized tracked-files-only context.
3. The canonical Compose API still uses `uvicorn --reload` and the existing local environment
   profile; this task intentionally did not change runtime semantics.

These do not block one explicitly approved, monitored daily canary, but they do not authorize broad
writer re-enablement.

## Approval gate

`READY_FOR_CONTROLLED_DAILY_CANARY`

No writer may run until the user explicitly approves the canary's exact write and external-API
scope.
