# TASK-20260901-042 — Dashboard Changed-File Ruff Debt Cleanup

## Objective

Clear the existing Ruff violations in only:

- `app/dashboard/renderer.py`
- `app/dashboard/types.py`

The cleanup must preserve Dashboard rendering behavior and the Dashboard types/serialization contract.

## Allowed scope

- Record the current Ruff rule/line/message inventory for the two files.
- Apply Ruff safe fixes.
- Make minimal manual lint-only edits where safe fixes are insufficient.
- Add focused regression tests only if required to prove behavior preservation.
- Run focused, unit, integration, Ruff, Black, MyPy, and diff checks.
- Create an independent PR to `master` and stop at the Merge Approval Gate.

## Prohibited scope

- Do not use `ruff --unsafe-fixes`.
- Do not modify files outside the two target files except task/report/handoff artifacts or strictly necessary tests.
- Do not modify PR #71 or cherry-pick TASK-20260901-041.
- Do not alter Dashboard behavior, dataclass/schema/serialization contracts, production data, scheduler configuration, API configuration, database schema, or Frozen betting logic.

## Completion status

- Recorded 46 baseline Ruff violations across the two files.
- Applied 37 Ruff safe fixes and 9 minimal manual fixes; unsafe fixes were not used.
- Both changed files now pass Ruff, Black, and strict changed-file MyPy.
- Fixed-input Dashboard HTML is byte-for-byte identical before and after cleanup.
- Dashboard dataclass field contract is unchanged.
- Dashboard focused tests, full unit tests, and integration tests pass.
- PR #72 is open with all required checks passing.
- Stopped at the Merge Approval Gate; no merge was performed.

## Safety

- Production database writes: 0
- External API calls: 0
- Production jobs: 0
- Windows Scheduler changes: 0
- Frozen items: unchanged
