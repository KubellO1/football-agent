# TASK-20260828-023 — Fix expired date-sensitive unit test

## 状态

completed — awaiting PR checks and merge approval

## 实施

- 仅在测试中冻结 `time.time()` 到供应商 reset 时间之前。
- 保留原始 rate-limit header 样例及所有断言。
- 生产代码改动为 0。

## 验证

- focused：1 passed。
- CI unit command：658 passed。
- Ruff：PASS。
- Black：PASS。
- MyPy：不适用；本任务没有修改 `app/*`，CI Quality 不会调用 MyPy。

## Frozen

模型、EV、Kelly、Confidence、Gate、Whitelist、Stake、数据库结构、计划任务及投注决策规则均未修改。

## Approval Gate

独立 PR required checks 全部通过后 STOP，等待合并批准。
