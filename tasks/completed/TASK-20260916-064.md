# TASK-20260916-064 — Controlled Production Re-enable after Recovery

## Status

Stopped at the writer re-enable approval gate. Redis is running and healthy. A temporary,
non-public clean-release API container passed read-only liveness/readiness checks and was removed.
The existing production API container remains stopped because it mounts the old dirty working tree.

## Findings

- Production PostgreSQL is healthy at canonical Alembic 0021.
- Redis is running and healthy.
- Four canonical database-writer Scheduler tasks remain Disabled.
- No production writer or Alembic process is running.
- Existing production API container bind-mounts the dirty legacy working tree.
- All canonical Scheduler tasks reference a release virtual-environment launcher whose base Python
  no longer exists.

## Limits observed

No writer task was enabled, no production job/model/backfill/collector ran, no historical gap was
repaired, no external API was called, and Alembic 0022 remains frozen.

See reports/codex/TASK-20260916-064_result.json and .md.
