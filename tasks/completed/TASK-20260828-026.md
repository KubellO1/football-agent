# TASK-20260828-026 — Fix Odds-API.io 1x2 → h2h ingestion contract

## Objective

在 Odds-API.io provider normalization 边界把精确的 1X2/moneyline 市场别名映射为内部 canonical `h2h`，并证明真实 provider payload 能进入 snapshot persistence 调用边界。

## Allowed scope

- Odds-API.io 市场 normalization。
- 未知市场的结构化 reason code、日志和可审计计数。
- provider、normalization、ingestion 与 snapshot persistence 边界测试。
- 任务报告、handoff 与独立 PR。

## Forbidden scope observed

- EV、Kelly、Confidence、RecommendationGate、`min_bookmakers` 和 bookmaker 配置均未修改。
- 外部 API 调用：0。
- Production PostgreSQL 写入：0。
- Production daily_job、pre_kickoff、backfill、migration、deployment：均未运行。
- Alembic stamp、upgrade、downgrade：均未运行。
- Frozen 项保持不变。
- 未合并 master。

## Result

- Odds-API.io 的精确三向 moneyline 别名在 provider normalization 边界统一输出 `h2h`。
- `double chance`、`draw no bet`、`DNB`、亚洲盘和 totals 不会被误映射。
- 未知市场产生 `ODDS_API_IO_UNSUPPORTED_MARKET` reason code 日志，并累计 `markets_rejected_unsupported` provider 计数。
- 真实 Odds-API.io 形状的 MockTransport payload 已通过 fixture matching，并在隔离 PostgreSQL 中创建 home/draw/away 三条 snapshot。
- 所有 required 本地检查通过。

## Approval gate

PR 创建后 STOP 在 Merge Approval Gate。不得自动合并 master。
