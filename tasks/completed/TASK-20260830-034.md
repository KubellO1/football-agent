# TASK-20260830-034 — Declare Windows tzdata Runtime Dependency

## Objective

Declare the Windows IANA timezone database dependency so a clean Python 3.12 runtime installed only from the project manifest can load `Europe/Paris` and import the production scheduler.

## Allowed scope

- Add a conditional Windows `tzdata` runtime dependency to `pyproject.toml`.
- Add the minimum packaging/runtime contract test.
- Build a disposable clean Windows virtual environment from the project manifest.
- Run focused, unit, integration and quality verification.
- Create a pull request to `master` and stop at the Merge Approval Gate.

## Prohibited

- Global package installation or an undeclared manual runtime workaround.
- Scheduler logic, `ZoneInfo` usage or cadence changes.
- Database schema or production database writes.
- Provider behavior or configuration changes.
- Production job execution or Windows scheduled-task changes.
- Merge to `master`.

## Frozen

- Model weights and alpha.
- EV and confidence thresholds.
- Kelly formula and cap.
- Recommendation Gate.
- Production whitelist.
- Betting decision rules.

## Completion status

- Conditional Windows runtime dependency declared.
- Clean Windows Python 3.12.10 virtual-environment contract passed with `tzdata==2026.3` installed by the project manifest.
- Focused, unit and integration tests passed.
- Stopped at the pull-request Merge Approval Gate.
