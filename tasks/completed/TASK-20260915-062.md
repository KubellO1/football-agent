# TASK-20260915-062 — Production Database Recovery Safety + Disposable Rehearsal

## Outcome

Completed and stopped at `PRODUCTION_DATABASE_RECOVERY_APPROVAL_GATE`.

The integration-test DSN defect was fixed on `codex/TASK-20260915-062`. Missing or unsafe
`TEST_DATABASE_URL` now fails before database mutation, the application DSN is never a test
fallback, actual connected identity is checked before metadata DDL, and the CI test migration has
the same preflight guard.

The best preserved production-origin backup was restored twice into independent disposable
databases. Both rehearsals upgraded 0020 → 0021 and produced identical canonical schema/data
hashes, zero canonical duplicates, zero FK orphans, clean Alembic checks and successful read-only
application compatibility.

## Gate

Production remains untouched. Recovery requires explicit approval after accepting or separately
addressing the minimum known gap of 95 fixtures, 7 predictions and one bookmaker. Alembic 0022,
collectors, backfill and all production writers remain frozen.

See:

- `reports/codex/TASK-20260915-062_result.json`
- `reports/codex/TASK-20260915-062_result.md`

