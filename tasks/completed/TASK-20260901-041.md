# TASK-20260901-041 — Dashboard data-chain audit and display correction

## Status

Completed locally; stopped before PR/merge/deployment approval.

## Objective

Audit why `dashboard_2026-09-01.html` renders two production fixtures as
`Home vs Away`, verify the fixture/team data chain read-only, display the
configured production minimum EV threshold, and audit the `providers=0/0`
health display.

## Allowed scope

- Read dashboard artifact, application code, configuration names, logs, and
  production PostgreSQL under a read-only transaction.
- Correct Dashboard data loading and presentation defects.
- Add focused tests and task reports.

## Prohibited scope respected

- Production database writes or production-data correction: none.
- Model, odds, EV, Kelly, confidence, Gate, whitelist, stake, or betting-rule
  changes: none.
- Scheduler definitions, cadence, or production job execution: none.
- Provider-health producer changes: none; root cause documented separately.

## Outcome

- Root cause classified as **C — db_builder did not read team names**.
- Dashboard now resolves team/competition labels from canonical reference rows,
  retaining prediction text only as a compatibility fallback.
- Empty recommendation text reads the configured `recommendations_min_ev`
  (production value `0.05`) and renders `EV ≥ 5%`.
- Provider health `0/0` is a separate producer/consumer contract problem and
  was not changed.

## Approval gate

STOP before PR creation, merge, deployment, production job run, or provider
health repair.
