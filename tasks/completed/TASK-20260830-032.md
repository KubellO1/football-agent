# TASK-20260830-032 — Alembic 0021 Provider Provenance Production Clone Rehearsal

## 目标

仅在 production clone / disposable PostgreSQL 中验证 TASK-20260830-031 引入的 Alembic 0021，确认它能否安全应用于当前 production revision 0020。

## 范围

- 对 production PostgreSQL 仅做只读审计与备份。
- 从同一份最新 production 备份创建两个独立 clone。
- 在每个 clone 上执行 `0020 → 0021 → 0020 → 0021`。
- 验证 schema、行数、重复自然键、sequence、provenance 和应用兼容性。
- 禁止 production migration、生产任务、真实 Canary、部署、合并及 Frozen 修改。

## 结果

- 0021 审计通过：仅为 `odds_snapshots` 增加两个 nullable、无默认值的 provenance 字段和一个普通索引。
- 两轮独立 rehearsal 均通过，最终 schema hash 完全一致。
- downgrade/recovery rehearsal 通过，恢复后的 0020 schema 与初始 0020 hash 一致。
- 历史赔率行不会被修改或删除；未知历史来源保持 `NULL`，没有伪造 provider。
- focused/integration 兼容路径 47 passed；完整 integration 103 passed；完整 unit 686 passed。
- 增量 Ruff 与 MyPy 通过；`git diff --check` 通过。
- TASK-031 继承文件 `tests/unit/test_prioritized_odds_fallback.py` 在当前 Black 版本下存在格式漂移，本任务未修改该无关文件。
- production database writes = 0；external API calls = 0；production jobs run = 0。

## Approval Gate

Clone rehearsal 已完成。停止执行，等待明确的 Production Database Approval。不得自动执行 production 0021、TASK-031 production Canary、部署或合并。
