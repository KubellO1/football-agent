# TASK-20260915-063 — Production Database Recovery Execution

## Status

Stopped before recovery because canonical Windows Scheduler task state cannot be read with the
current Windows token (`Access is denied`). Do not restart production PostgreSQL or cut over until
an Administrator context verifies that every PostgreSQL-writing task is frozen.

## Preserved forensic evidence

- Cold read-only copy of stopped `football-agent_pgdata` volume.
- Readable custom-format `pg_dump` from a no-network clone of that cold volume.
- Original damaged volume and database unchanged.

## Still required

1. Administrator Scheduler inventory and writer freeze confirmation.
2. Recheck no processes/locks.
3. Direct live forensic production `pg_dump` after freeze.
4. Exact TASK-062 restore/upgrade/cutover verification.

See `reports/codex/TASK-20260915-063_result.json` and `.md`.
