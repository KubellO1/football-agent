# TASK-20260828-026 — Fix Odds-API.io 1x2 → h2h ingestion contract

## Objective

在 Odds-API.io provider normalization 边界把精确的 1X2/moneyline 市场别名映射为内部 canonical `h2h`，并证明真实 provider payload 能进入 snapshot persistence 调用边界。

## Allowed scope

- Odds-API.io 市场 normalization。
- 未知市场的结构化 reason code、日志和可审计计数。
- provider、normalization、ingestion 与 snapshot persistence 边界测试。
- 任务报告、handoff 与独立 PR。

## Forbidden scope

- 不修改 EV、Kelly、Confidence、RecommendationGate、`min_bookmakers` 或 bookmaker 配置。
- 不调用外部 API。
- Production PostgreSQL 写入为 0。
- 不运行 production daily_job、pre_kickoff、backfill、migration、deployment。
- 不执行 Alembic stamp、upgrade 或 downgrade。
- Frozen 项保持不变。
- 不自动合并 master。

## Approval gate

完成实现、验证、报告和 PR 后，STOP 在 Merge Approval Gate。
