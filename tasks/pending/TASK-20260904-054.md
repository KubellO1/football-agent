# TASK-20260904-054 — Zero-Cost Research Dataset Manifest + Baseline Experiment Runner

## 目标

在新增外部数据成本为 0 的前提下，建立 research-only 数据清单、统一时序契约和确定性 baseline runner，评估现有合法数据能否支持五大联赛 OOS 概率质量验证。

## 允许范围

- 复用 TASK-20260904-053 的零成本架构契约。
- 登记合法、可复用的研究数据资产及 provenance/license。
- 新增与 production wiring 隔离的 research-only 代码和测试。
- 在已有本地样本及合成测试夹具上验证时序、去重和确定性。

## 禁止范围

- 不购买任何数据或服务。
- 不调用外部 API。
- 不写 production PostgreSQL。
- 不修改 production model、90% completeness Gate、Scheduler 或任何 Frozen 项。
- 不生成正式 BET，不合并 master，不部署。

## 验收

- 数据 manifest 可验证且不会把 research-only 误标为 production-compatible。
- 统一 fixture/team metric schema 明确保留 NULL，禁止 goals 代替 xG。
- 5/10/20 窗口、chronological split、去重与 leakage 防护可测试。
- A–F baseline、Brier、Log Loss、calibration、accuracy 和 ablation 可重复运行。
- 无同期合法赔率时输出 `BETTING_SIMULATION=INSUFFICIENT_ODDS_DATA`。
- 输出标准 JSON、Markdown、completed task 和 handoff 后停止。

## Approval Gate

`ZERO_COST_MODEL_FEASIBILITY_APPROVAL_GATE`
